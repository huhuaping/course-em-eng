# # global options for DT
# options(
#   DT.options = list(
#     dom ="t" ,  # pure table with no search blank
#     rownames = FALSE, # 不显示行标签
#     columnDefs = list(
#       list(className = "dt-center", targets = "_all"), # align center
#       list(visible=FALSE,targets=0) # hide index column
#       )
# )
# )